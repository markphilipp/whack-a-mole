# Local state / logs (shouldn't normally live here, but just in case)
state.json
logs/
*.log

# Worktrees created for dispatched jobs
worktrees/

# Local config (real paths / identity). Template is config.yaml.example.
config.yaml

# Editor cruft
.DS_Store
.vscode/
.idea/
*.swp
